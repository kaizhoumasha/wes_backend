services:
  db:
    image: timescale/timescaledb:latest-pg17
    container_name: wes_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - ./docker_data/postgres:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${POSTGRES_USER}" ]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - wesp9-network

  redis:
    image: redis:8-alpine
    container_name: wes_redis
    restart: unless-stopped
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - ./docker_data/redis:/data
    command: redis-server --requirepass ${REDIS_PASSWORD}
    healthcheck:
      test: [ "CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping" ]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - wesp9-network

# # ============================================
# # Named Volumes (Data Persistence)
# # ============================================
# volumes:
#   postgres_data:
#     driver: local
#     labels:
#       com.wesp9.description: "PostgreSQL 数据持久化卷"
#       com.wesp9.edition: "all"

#   redis_data:
#     driver: local
#     labels:
#       com.wesp9.description: "Redis 数据持久化卷"
#       com.wesp9.edition: "enterprise,flagship"

# ============================================
# Networks
# ============================================
networks:
  wesp9-network:
    driver: bridge
    labels:
      com.wesp9.description: "WES P9 内部通信网络"
