"""注册到 `src/workline_plugin_registry.py` 的片段。

把下面片段放在 `WORKLINE_PLUGIN_REGISTRY` 定义之后；或把条目合并进字典字面量。
"""

from src.workline_plugin_registry import WORKLINE_PLUGIN_REGISTRY, WorklinePluginDefinition

WORKLINE_PLUGIN_REGISTRY.update(
    {
        "{{PLUGIN_KEY}}": WorklinePluginDefinition(
            plugin_key="{{PLUGIN_KEY}}",
            plugin_module="src.workline_plugins.{{PLUGIN_MODULE}}.plugin",
            plugin_class_name="{{PLUGIN_CLASS}}",
        ),
    }
)
