# SMT 粗分机联调手册（供应商版）

> 面向设备侧开发、现场调试与联调验收。
> 适用系统：WES 后端 / SMT 粗分机插件 `rough_sorter.v1`。
> 适用设备：粗分机入料机械臂、输送线、出料机械臂。
> 文档版本：v1.0。

---

## 0. 快速联调路径

先按这个顺序跑。不要一开始就接真实设备；网络、签名、设备动作混在一起时，排障会很慢。

1. WES 项目组开通硬件方账号和设备管理权限。
2. 硬件方在本系统管理界面维护设备主数据：`device_code`、角色、`host`、`port`、`protocol`、`callback_path`。
3. 硬件方用 `curl` 调通 `POST /api/v1/callback/event`，确认 API 签名正确。
4. WES 调试人员使用 Sandbox 生成待执行命令，硬件方对照命令包络实现设备命令接收服务。
5. 硬件方设备收到 WES 命令后先返回 HTTP 200 ACK，不在 ACK 阶段执行长耗时动作。
6. 设备动作完成后调用 `POST /api/v1/callback/result` 回传 Result。
7. 双方通过 Trace 核对 Session、Command、Outbox、Inbox、Timeline 是否推进到预期状态。

本文中涉及三类调用方式：

| 调用方向 | 接口 | 认证方式 | 用途 |
| --- | --- | --- | --- |
| 设备 -> WES | `/api/v1/callback/event`、`/api/v1/callback/result` | `APP_ID` / `APP_SECRET` 签名 | 真实设备上报事件和结果 |
| WES -> 设备 | `{protocol}://{host}:{port}{callback_path}` | 当前默认不带签名 Header | WES 下发设备动作命令 |
| WES 调试人员 -> WES | `/api/v1/workline/operations/sandbox/*` | 后台用户 `Bearer Token` | 无真实设备时模拟 ACK / Result |

---

## 1. 联调目标

这份文档只说明粗分机和 WES 的 HTTP 对接。联调时重点看这几件事：

- 设备向 WES 上报扫码事件。
- WES 向设备下发粗分机动作命令。
- 设备返回 HTTP ACK。
- 设备执行完成后回传 Result。
- 业务 NG 与硬件失败的区分。
- 使用 `CURL + Sandbox + Mock` 先跑通无设备流程。
- 使用 Trace 接口定位联调问题。

粗分机正常主路径：

```text
SCAN_COMPLETED
  -> MEASUREMENT_REEL
  -> PICK_AND_PUT
  -> MOVE_FORWARD
  -> PUT_TO_BIN
  -> Session COMPLETED
```

业务 NG 路径：

```text
SCAN_COMPLETED 或 MEASUREMENT_REEL 判定 NG
  -> MOVE_TO_NG
  -> Session COMPLETED 或人工处理
```

---

## 2. 环境与接口地址

当前联调地址如下。WES 项目组如果临时切换域名、端口或 API 前缀，以现场通知为准。

### 2.1 本地工具要求

调试机准备这些工具：

- `curl`：发送 HTTP 请求。
- `jq`：格式化响应并提取字段。
- `openssl`：生成 HMAC-SHA256 签名。

### 2.2 通用环境变量

```bash
export WES_API=https://mcs.happytable.cc/api/v1
export APP_ID=app_7hBRU_aYUnM8ZY2S
export APP_SECRET="<由 WES 项目组通过安全渠道提供>"
```

| 项目 | 示例 | 说明 |
| --- | --- | --- |
| WES API Base | `https://mcs.happytable.cc/api/v1` | 所有 WES API 基础地址 |
| API App ID | `app_7hBRU_aYUnM8ZY2S` | 设备回调接口签名使用 |
| API App Secret | 通过安全渠道提供 | 不要写入长期留存文档或代码仓库 |
| 设备事件回调 | `/api/v1/callback/event` | 设备上报扫码、传感器、状态类事件 |
| 设备结果回调 | `/api/v1/callback/result` | 设备完成 WES 指令后的结果回传 |
| WES 下发设备命令 | `{protocol}://{host}:{port}{callback_path}` | URL 来自 WES 设备主数据 |
| 默认设备命令路径 | `/api/v1/device/command` | 可按设备配置覆盖 |
| Sandbox 待处理命令 | `/api/v1/workline/operations/sandbox/pending` | 仅 WES 调试/Sandbox 使用 |

---

## 3. API 签名认证

正式联调环境使用 API App 签名。开发环境如果 WES 临时打开 `SKIP_API_AUTH=True`，可以跳过签名；供应商程序仍按正式签名实现。

### 3.1 请求 Header

| Header | 必填 | 说明 |
| --- | --- | --- |
| `X-App-ID` | 是 | WES 分配给供应商或设备系统的应用 ID |
| `X-Timestamp` | 是 | Unix 秒级时间戳 |
| `X-Signature` | 是 | HMAC-SHA256 小写十六进制签名 |
| `Content-Type` | 是 | 固定为 `application/json` |

### 3.2 签名规则

签名原文：

```text
{app_id}{timestamp}{HTTP_METHOD}{request_path}
```

规则：

- `HTTP_METHOD` 使用大写，例如 `POST`。
- `request_path` 只包含路径，例如 `/api/v1/callback/result`。
- 签名不包含 body，避免 JSON 序列化差异导致验签失败。
- `X-Timestamp` 放在 WES 当前时间前后可接受窗口内。
- `APP_SECRET` 只放在本机环境变量或设备安全配置里，不要写进代码、工单、群聊截图、日志或长期留存文档。

Bash 示例：

```bash
APP_ID="app_7hBRU_aYUnM8ZY2S"
APP_SECRET="<由 WES 项目组通过安全渠道提供>"
TS=$(date +%s)
METHOD="POST"
PATH="/api/v1/callback/result"

SIGN=$(printf "%s" "${APP_ID}${TS}${METHOD}${PATH}" \
  | openssl dgst -sha256 -hmac "$APP_SECRET" -hex \
  | awk '{print $2}')
```

联调时可以先定义一个函数，后面 Event 和 Result 请求都能复用：

```bash
sign_wes_request() {
  local method="$1"
  local path="$2"
  local ts="$3"

  printf "%s" "${APP_ID}${ts}${method}${path}" \
    | openssl dgst -sha256 -hmac "$APP_SECRET" -hex \
    | awk '{print $2}'
}
```

---

## 4. 粗分机设备角色

粗分机工作线需要三类设备。少任意一类，后续命令都会卡住。

| 设备角色 | 测试 `device_code` | 支持事件/命令 | 说明 |
| --- | --- | --- | --- |
| `ROUGH_SORTER_INPUT_ARM` | `RS-INPUT-ARM-01` | `SCAN_COMPLETED`, `MEASUREMENT_REEL`, `PICK_AND_PUT`, `MOVE_TO_NG` | 入料机械臂，负责扫码入口、测量、入线抓取和 NG 搬运 |
| `ROUGH_SORTER_CONVEYOR` | `RS-CONVEYOR-01` | `MOVE_FORWARD` | 输送线，负责将物料从入料端移动到出料端 |
| `ROUGH_SORTER_OUTPUT_ARM` | `RS-OUTPUT-ARM-01` | `PUT_TO_BIN` | 出料机械臂，负责将物料放入目标料箱格位 |

联调前先准备这些信息，并由硬件方在本系统管理界面维护到对应设备基础信息中：

- 每台设备的 `host`、`port`、`protocol`、`callback_path`。
- 每台设备支持的 `task_type` 清单。
- 最大并发、超时时间、幂等策略。
- 设备错误码字典。

`host`、`port`、`protocol`、`callback_path` 是 WES 下发命令时使用的设备连接配置。硬件方负责在本系统管理界面自行维护；WES 项目组负责开通账号权限、确认字段口径和联调结果。

`callback_path` 是设备基础信息里的命令接收路径。不填时，WES 使用默认路径 `/api/v1/device/command`。

例如供应商服务地址是 `http://10.0.0.10:9000/api/device/command`，设备主数据填：

| 字段 | 示例 |
| --- | --- |
| `protocol` | `HTTP` |
| `host` | `10.0.0.10` |
| `port` | `9000` |
| `callback_path` | `/api/device/command` |

---

## 5. 设备上报 Event 协议

### 5.1 `SCAN_COMPLETED` 扫码完成事件

扫码完成后，设备调用：

```text
POST /api/v1/callback/event
```

业务字段放在 `data` 里，不要把六合一码字段放到顶层。

`PkgID` 是粗分机的业务主键。这个值要稳定、唯一、非空。

| 字段 | 位置 | 必填 | 说明 |
| --- | --- | --- | --- |
| `device_code` | 顶层 | 是 | 上报设备编码，必须能在 WES 中路由到对应 WorkLine |
| `event_type` | 顶层 | 是 | 固定为 `SCAN_COMPLETED` |
| `timestamp` | 顶层 | 建议 | Unix 毫秒时间戳 |
| `trace_id` | 顶层 | 建议 | 链路追踪 ID，现场排障用 |
| `event_id` | 顶层 | 建议 | 供应商事件唯一 ID，用于幂等和审计 |
| `data.HHPN` | `data` | 是 | 物料编码/SKU |
| `data.MfrPN` | `data` | 是 | 制造商料号 |
| `data.Qty` | `data` | 是 | 数量 |
| `data.DateCode` | `data` | 是 | 日期码 |
| `data.LotCode` | `data` | 是 | 批次 |
| `data.PkgID` | `data` | 是 | 包号/载具唯一号，粗分机业务主键来源 |
| `data.location` | `data` | 建议 | 扫码/测量位置 |

请求示例：

```bash
PATH="/api/v1/callback/event"
METHOD="POST"
TS=$(date +%s)
SIGN=$(sign_wes_request "$METHOD" "$PATH" "$TS")

curl -sS -X POST "$WES_API/callback/event" \
  -H "Content-Type: application/json" \
  -H "X-App-ID: $APP_ID" \
  -H "X-Timestamp: $TS" \
  -H "X-Signature: $SIGN" \
  -d '{
    "device_code": "RS-INPUT-ARM-01",
    "event_type": "SCAN_COMPLETED",
    "timestamp": 1777200000000,
    "trace_id": "trace-rough-sorter-0001",
    "event_id": "evt-scan-0001",
    "data": {
      "location": "ARM01",
      "HHPN": "CAP001",
      "MfrPN": "V0001-CAP-0402",
      "Qty": "100",
      "DateCode": "20260409",
      "LotCode": "LOT-A",
      "PkgID": "PKG-CAP001-LOT-A-001"
    }
  }'
```

返回示例：

```json
{
  "code": 200,
  "data": {
    "request_id": "...",
    "trace_id": "trace-rough-sorter-0001",
    "event_id": "evt-scan-0001",
    "causation_id": null
  }
}
```

说明：HTTP 200 只表示 WES 已接收事件，不表示整条工作线已经完成。

---

## 6. WES 下发设备 Command 协议

WES 按设备主数据拼出命令地址，然后发送 HTTP POST：

```text
{protocol}://{host}:{port}{callback_path}
```

默认 `callback_path`：

```text
/api/v1/device/command
```

WES 下发设备命令时，不带 `X-App-ID` / `X-Signature` 这组回调签名 Header。WES 只按设备主数据中的地址发 HTTP POST。设备侧如果要求认证、白名单、网关 Token 或 mTLS，联调前先和 WES 项目组约定，否则设备可能直接拒收命令。

### 6.1 ACK 规则

WES 以设备返回 HTTP 200 作为 ACK 成功。

设备可以返回：

```json
{
  "code": 200,
  "message": "OK",
  "trace_id": "device-local-log-20260530-001"
}
```

非 200、网络异常或超时会触发 WES 重试和诊断。

### 6.2 通用 Command 包络

设备程序只按 `command_code`、`task_type`、`params` 解析动作。`priority`、`timeout`、`timestamp` 是调度辅助字段，可以记日志或做超时处理，不要用它们决定业务动作。

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `command_code` | 是 | WES 生成的全局唯一命令号，Result 原样回传 |
| `task_type` | 是 | 命令类型 |
| `params` | 是 | 业务参数对象 |
| `priority` | 辅助 | 命令优先级 |
| `timeout` | 辅助 | WES 给出的超时时间，单位毫秒 |
| `timestamp` | 辅助 | WES 下发时间，Unix 毫秒 |

### 6.3 粗分机 `task_type`

| `task_type` | 目标设备角色 | `params` 核心字段 | 完成后下一步 |
| --- | --- | --- | --- |
| `MEASUREMENT_REEL` | `ROUGH_SORTER_INPUT_ARM` | `business_key`, `six_in_one`, `trace_id` | 设备测量后回传 `reel_diameter` / `reel_thickness` |
| `PICK_AND_PUT` | `ROUGH_SORTER_INPUT_ARM` | `business_key`, `source_location`, `target_location` | 成功后 WES 下发 `MOVE_FORWARD` |
| `MOVE_FORWARD` | `ROUGH_SORTER_CONVEYOR` | `business_key`, `source_location`, `target_location` | 成功后 WES 尝试分配料箱；分配成功才下发 `PUT_TO_BIN` |
| `PUT_TO_BIN` | `ROUGH_SORTER_OUTPUT_ARM` | `business_key`, `source_location`, `target_location`, `bin_location` | 成功后 WES 完成 Session 并记录资源事实 |
| `MOVE_TO_NG` | `ROUGH_SORTER_INPUT_ARM` | `business_key`, `source_location`, `target_location`, `ng_location`, `reason_code` | 成功后完成当前物料流程 |

### 6.4 `MEASUREMENT_REEL` 命令示例

```json
{
  "command_code": "CMD-20260530-MEASUREMENT_REEL-ABC12345",
  "task_type": "MEASUREMENT_REEL",
  "params": {
    "business_key": "PKG-CAP001-LOT-A-001",
    "six_in_one": {
      "HHPN": "CAP001",
      "MfrPN": "V0001-CAP-0402",
      "Qty": "100",
      "DateCode": "20260409",
      "LotCode": "LOT-A",
      "PkgID": "PKG-CAP001-LOT-A-001"
    },
    "trace_id": "trace-rough-sorter-0001"
  }
}
```

---

## 7. 设备回传 Result 协议

设备完成动作后调用：

```text
POST /api/v1/callback/result
```

### 7.1 Result 字段

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `command_code` | 是 | 必须等于 WES 下发命令中的 `command_code` |
| `device_code` | 是 | 执行该命令的设备编码，必须与 WES 命令目标设备一致 |
| `result` | 是 | `SUCCESS` 或 `FAILED` |
| `finish_time` | 是 | Unix 毫秒完成时间 |
| `data` | `SUCCESS` 时建议 | 业务结果数据 |
| `error_detail` | `FAILED` 时必填 | 设备动作失败、硬件异常、位置不确定等错误详情 |
| `trace_id` / `event_id` / `causation_id` | 建议 | 链路追踪字段 |

排障和补偿时先看 `command_code`。设备即使没有回传 `trace_id`，WES 也会按 `command_code` 找回上下文。

### 7.2 `MEASUREMENT_REEL` 成功示例

```bash
PATH="/api/v1/callback/result"
METHOD="POST"
TS=$(date +%s)
SIGN=$(sign_wes_request "$METHOD" "$PATH" "$TS")

curl -sS -X POST "$WES_API/callback/result" \
  -H "Content-Type: application/json" \
  -H "X-App-ID: $APP_ID" \
  -H "X-Timestamp: $TS" \
  -H "X-Signature: $SIGN" \
  -d '{
    "command_code": "CMD-20260530-MEASUREMENT_REEL-ABC12345",
    "device_code": "RS-INPUT-ARM-01",
    "result": "SUCCESS",
    "finish_time": 1777200060000,
    "trace_id": "trace-rough-sorter-0001",
    "event_id": "evt-measure-result-0001",
    "data": {
      "reel_diameter": "178.0",
      "reel_thickness": "15.0"
    }
  }'
```

### 7.3 测量业务 NG

设备动作成功，但检测结果判定为 NG 时，不要回 `FAILED`。

回 `SUCCESS`，在 `data` 里带业务 NG 字段。

```json
{
  "command_code": "CMD-20260530-MEASUREMENT_REEL-ABC12345",
  "device_code": "RS-INPUT-ARM-01",
  "result": "SUCCESS",
  "finish_time": 1777200060000,
  "data": {
    "reel_diameter": "178.0",
    "reel_thickness": "15.0",
    "inspection_result": "NG",
    "ng_reason": "INSPECTION_SIZE_NG"
  }
}
```

下面任一字段为 `NG`，WES 都按业务 NG 处理：

- `measurement_result`
- `inspection_result`
- `size_judgement`
- `thickness_judgement`

之后 WES 会生成业务 NG，并下发 `MOVE_TO_NG`。

### 7.4 设备动作失败

只有设备未完成动作、位置不确定、抓取失败、放置失败、硬件报警等情况，才回 `FAILED`。

```json
{
  "command_code": "CMD-20260530-PICK_AND_PUT-DEF67890",
  "device_code": "RS-INPUT-ARM-01",
  "result": "FAILED",
  "finish_time": 1777200090000,
  "error_detail": {
    "code": "PICK_AND_PUT_FAILED",
    "message": "夹爪取料失败，物料位置不确定",
    "retryable": false
  }
}
```

`FAILED` 会让 WES 进入阻断、对账或人工处理流程。

---

## 8. Sandbox + CURL 无真实设备联调

真实设备还没接入时，先让 WES 调试人员用 `SIMULATION` 工作线模拟事件和结果。

Sandbox 会创建真实的 Session、Command、Outbox、Inbox、Timeline 和 Trace，但不会访问真实设备地址。

注意字段名差异：

| 场景 | 接口 | 结果数据字段 |
| --- | --- | --- |
| 真实设备回传 Result | `/api/v1/callback/result` | `data` |
| Sandbox 模拟 Result | `/api/v1/workline/operations/results` | `payload` |

### 8.1 启动环境

```bash
./scripts/init-env.sh dev
docker-compose up -d
uv sync --dev
./scripts/migrate.sh upgrade

uv run uvicorn main:app --reload --host 0.0.0.0 --port 8001
sh src/celery_app/dev_worker_autoreload.sh
```

### 8.2 同步粗分机测试工作线和设备

```bash
uv run python scripts/data/sync_test_workline_devices.py
```

脚本会准备这些测试数据：

- WorkLine：`WL-ROUGH-SORTER-TEST`
- 入料机械臂：`RS-INPUT-ARM-01`
- 输送线：`RS-CONVEYOR-01`
- 出料机械臂：`RS-OUTPUT-ARM-01`

### 8.3 出料入箱前置条件

`MEASUREMENT_REEL`、`PICK_AND_PUT`、`MOVE_FORWARD` 主要验证设备命令交互。`MOVE_FORWARD` 成功后，WES 会做出料分配；能不能继续生成 `PUT_TO_BIN`，取决于资源域配置。

要跑到 `PUT_TO_BIN -> Session COMPLETED`，测试环境需要满足：

- 已注入粗分机 `bin_allocator` 服务。
- 当前工作线存在可用料箱、料格或有效的 active bin rack 投影。
- 料箱分配器能为当前 `PkgID` 返回 `ALLOCATED` 决策。

如果条件不满足，Sandbox 可能在 `MOVE_FORWARD` 成功后进入 `MANUAL_HOLD` 或 blocking-point，不会继续生成 `PUT_TO_BIN`。这是 WES 资源配置问题，不算设备协议失败。

### 8.4 获取后台调试 Token

Sandbox 是 WES 后台调试接口，使用后台用户 Token，不使用设备侧 `APP_ID` / `APP_SECRET` 签名。

本地开发环境可以用初始化管理员账号：

```bash
export WES_API=http://localhost:8001/api/v1
export WES_TOKEN=$(
  curl -sS -X POST "$WES_API/auth/login" \
    -H "Content-Type: application/json" \
    -d '{"username":"admin","password":"admin123"}' \
  | jq -r '.data.access_token'
)
```

如果在正式联调地址 `https://mcs.happytable.cc/api/v1` 操作 Sandbox，让 WES 项目组提供具备 `biz:workline:list` 和 `biz:workline:update` 权限的后台用户 Token：

```bash
export WES_API=https://mcs.happytable.cc/api/v1
export WES_TOKEN="<由 WES 项目组提供>"
```

### 8.5 查询 WorkLine 和设备 ID

```bash
curl -sS "$WES_API/workline/runtime/worklines?excludeSimulation=false" \
  -H "Authorization: Bearer $WES_TOKEN" | jq

export WID=<WL-ROUGH-SORTER-TEST 的 id>

curl -sS "$WES_API/workline/runtime/devices?worklineId=$WID" \
  -H "Authorization: Bearer $WES_TOKEN" | jq

export INPUT_ARM_ID=<RS-INPUT-ARM-01 的 id>
```

### 8.6 发送 Sandbox 扫码事件

```bash
export TRACE_ID="rough-sorter-curl-$(date +%s)"

curl -sS -X POST "$WES_API/workline/operations/sandbox/events" \
  -H "Authorization: Bearer $WES_TOKEN" \
  -H "Content-Type: application/json" \
  -d "{
    \"workline_id\": $WID,
    \"device_id\": $INPUT_ARM_ID,
    \"event_type\": \"SCAN_COMPLETED\",
    \"trace_id\": \"$TRACE_ID\",
    \"payload\": {
      \"device_code\": \"RS-INPUT-ARM-01\",
      \"data\": {
        \"location\": \"ARM01\",
        \"HHPN\": \"CAP001\",
        \"MfrPN\": \"V0001-CAP-0402\",
        \"Qty\": \"100\",
        \"DateCode\": \"20260409\",
        \"LotCode\": \"LOT-A\",
        \"PkgID\": \"PKG-CAP001-LOT-A-001\"
      }
    }
  }" | jq
```

### 8.7 查询 Sandbox 待执行命令

```bash
curl -sS "$WES_API/workline/operations/sandbox/pending?workline_id=$WID&limit=10" \
  -H "Authorization: Bearer $WES_TOKEN" | jq
```

每回传一次 Result 后，都重新查 pending，并从最新响应第一条取值：

```bash
export DISPATCH_KEY=<data[0].dispatch_key>
export COMMAND_CODE=<data[0].payload_json.command_code>
export DEVICE_CODE=<data[0].target_code>
export TASK_TYPE=<data[0].payload_json.task_type>
```

不要复用上一轮 `COMMAND_CODE`。`command_code` 只对应一条命令；复用旧值会导致 Result 被拒绝，或者落到错误命令上。

### 8.8 模拟 ACK

```bash
curl -sS -X POST "$WES_API/workline/operations/sandbox/ack" \
  -H "Authorization: Bearer $WES_TOKEN" \
  -H "Content-Type: application/json" \
  -d "{\"dispatch_key\":\"$DISPATCH_KEY\"}" | jq
```

### 8.9 模拟 Result

先看当前命令类型：

```bash
echo "$TASK_TYPE"
```

`MEASUREMENT_REEL` 成功结果：

```bash
curl -sS -X POST "$WES_API/workline/operations/results" \
  -H "Authorization: Bearer $WES_TOKEN" \
  -H "Content-Type: application/json" \
  -d "{
    \"command_code\": \"$COMMAND_CODE\",
    \"device_code\": \"$DEVICE_CODE\",
    \"result\": \"SUCCESS\",
    \"payload\": {
      \"reel_diameter\": \"178.0\",
      \"reel_thickness\": \"15.0\"
    }
  }" | jq
```

后续 `PICK_AND_PUT`、`MOVE_FORWARD`、`PUT_TO_BIN` 的成功结果可以先用空 payload：

```bash
curl -sS -X POST "$WES_API/workline/operations/results" \
  -H "Authorization: Bearer $WES_TOKEN" \
  -H "Content-Type: application/json" \
  -d "{
    \"command_code\": \"$COMMAND_CODE\",
    \"device_code\": \"$DEVICE_CODE\",
    \"result\": \"SUCCESS\",
    \"payload\": {}
  }" | jq
```

重复执行：

```text
查询 pending -> 取 command_code/target_code/dispatch_key -> ACK -> Result
```

重复到没有待处理命令，或 Trace 显示 `COMPLETED` / `MANUAL_HOLD`。

如果 `MOVE_FORWARD` 成功后没有新的 `PUT_TO_BIN`，先查 blocking-point。看到 `ROUGH_SORTER_ALLOCATOR_UNAVAILABLE`、`ROUGH_SORTER_ALLOCATION_BLOCKED` 或等待货架相关阻塞时，说明出料资源没配好。由 WES 项目组补齐资源域或料箱分配配置后，再跑正常主流程。

---

## 9. Mock WMS 校验

粗分机测量成功后会查 WMS 库存。Sandbox 正常流程使用：

| 字段 | 值 |
| --- | --- |
| `HHPN` | `CAP001` |
| `LotCode` | `LOT-A` |
| `PkgID` | `PKG-CAP001-LOT-A-001` |

启动 WMS Mock：

```bash
uv run python tests/mock/wms_mock_server.py
```

库存查询：

```bash
curl -sS -X POST "http://127.0.0.1:8011/api/wms/inventory/query" \
  -H "Content-Type: application/json" \
  -d '{"sku":"CAP001","lot_no":"LOT-A"}' | jq
```

返回的 `data.items` 应非空。

未知物料或批次会返回空库存，粗分机进入 `WMS_REJECTED` 业务 NG。

---

## 10. Trace 与诊断

联调时不要只看 HTTP 200。用 Trace 核对 Session、Command、Inbox、Outbox、Timeline 是否按顺序推进。

### 10.1 查询完整 Trace

```bash
curl -sS "$WES_API/workline/trace/trace/$TRACE_ID" \
  -H "Authorization: Bearer $WES_TOKEN" | jq
```

### 10.2 按 `command_code` 查询 Trace

```bash
curl -sS "$WES_API/workline/trace/command/$COMMAND_CODE" \
  -H "Authorization: Bearer $WES_TOKEN" | jq
```

### 10.3 查询阻塞点

```bash
curl -sS "$WES_API/workline/trace/$TRACE_ID/blocking-point" \
  -H "Authorization: Bearer $WES_TOKEN" | jq
```

### 10.4 查询设备路径视图

```bash
curl -sS "$WES_API/workline/runtime/traces/$TRACE_ID/path" \
  -H "Authorization: Bearer $WES_TOKEN" | jq
```

### 10.5 Trace 验收点

正常流程应能看到：

- `callback_logs` 中有 `event` 和后续 `result`。
- `inboxes` 已处理完成。
- `commands` 按顺序生成并完成。
- `outboxes` 已派发或 Sandbox 已处理。
- `sessions` 最终为 `COMPLETED`。
- 没有未处理的 blocking diagnostic。

---

## 11. 常见问题排查

| 现象 | 优先检查 | 处理建议 |
| --- | --- | --- |
| HTTP 401/403 | `X-App-ID`、`X-Timestamp`、`X-Signature`、权限 | 确认签名 path 是否正确；签名不包含 body |
| HTTP 422 | JSON 字段类型、必填字段、`data` 层级 | 业务字段必须放在 `data` 中 |
| Event 接收但没有命令 | 设备是否绑定 WorkLine，WorkLine 是否 active，`plugin_key` 是否 `rough_sorter` | 查询 Trace blocking-point 和工作线配置状态 |
| Result 被拒绝 | `command_code` 是否存在，`device_code` 是否匹配命令目标设备 | 不要复用旧 `command_code` |
| 业务 NG 被当成失败 | `result` 是否误传 `FAILED` | 测量 NG 应传 `SUCCESS`，并在 `data` 中给 NG 字段 |
| 命令不下发到真实设备 | WorkLine `run_mode` 是否 `SIMULATION` | `SIMULATION` 会进入 Sandbox pending，不会调用真实设备地址 |
| 设备拒收 WES 命令 | 设备侧命令接口是否要求额外认证 | 当前 WES 命令下发不带设备回调签名 Header；如需认证需提前约定 |
| 设备收到重复命令 | ACK 是否超时、WES 是否重试 | 设备必须按 `command_code` 做幂等 |
| WMS 校验不通过 | `HHPN` / `LotCode` 是否与库存一致 | Sandbox 正常流程使用 `CAP001` / `LOT-A` |
| `MOVE_FORWARD` 后不生成 `PUT_TO_BIN` | `bin_allocator`、active bin rack、料箱/料格资源是否可用 | 查询 blocking-point；先补齐 WES 资源域配置 |

---

## 12. 联调验收标准

下面项目都通过后，粗分机基础交互联调通过。

### 12.1 基础协议验收

- 设备成功上报 `SCAN_COMPLETED`，WES 返回 HTTP 200。
- WES 能按 `trace_id` 查到 callback log 和 `DEVICE_EVENT` inbox。
- 设备能接收 WES 下发的 `MEASUREMENT_REEL`、`PICK_AND_PUT`、`MOVE_FORWARD`、`PUT_TO_BIN` 或 `MOVE_TO_NG`。
- 设备用 HTTP 200 完成 ACK。
- 设备按 `command_code` 回传 `SUCCESS` result，WES 能推进下一步命令。
- 测量业务 NG 使用 `SUCCESS + data.NG` 表达，WES 进入 `MOVE_TO_NG`。
- 设备动作失败使用 `FAILED + error_detail` 表达，WES Trace 中能看到故障码和人工处理建议。

### 12.2 业务流程验收

- 出料资源配置满足时，正常主流程最终 Session 状态为 `COMPLETED`。
- Trace 中无未处理 pending outbox。
- `MOVE_FORWARD` 后如果资源域未满足，Trace 能清晰显示 blocking-point，双方能确认该阻塞不是设备协议问题。

---

## 13. 供应商交付清单

硬件方在本系统管理界面维护：

- 设备命令接收 HTTP 服务地址、端口、`callback_path`、协议 `http` / `https`。

硬件方同步给 WES 项目组确认：

- 每台设备的 `device_code`、角色、能力清单和最大并发。
- 设备命令接收接口的认证方式；如果不是纯内网或白名单 HTTP 200 ACK，需要提前提供认证方案。
- 命令 ACK 响应样例和设备内部 `trace_id` 规则。
- 每类 `task_type` 的成功 result 样例、失败 result 样例和业务 NG 样例。
- 设备错误码字典。
- 现场网络连通性说明：WES 到设备、设备到 WES 的防火墙和白名单。
- 幂等策略说明：同一 `command_code` 重复下发或同一 `event_id` / result 重复回调时设备侧处理方式。

---

## 14. 关键原则

- 业务字段只放在 `data` 或 `params` 中。
- `command_code` 由 WES 生成，设备不得自造或覆盖。
- `command_code` 是设备 Result 的唯一强锚点。
- 业务 NG 不等于设备失败。
- 设备失败才使用 `result=FAILED`。
- 设备必须按 `command_code` 实现幂等。
- 现场排障优先使用 Trace 和 blocking-point 接口。

---

## 15. 附录：最小设备 Mock 服务示例

可以先用下面的伪代码确认 WES 能否把命令打到设备侧服务。真实设备程序按供应商自己的框架实现，但行为保持一致：收到命令立即返回 HTTP 200 ACK，动作完成后再异步调用 WES Result。

```text
POST /api/v1/device/command
  1. 读取 JSON：command_code、task_type、params。
  2. 按 command_code 做幂等检查。
  3. 记录设备本地日志。
  4. 立即返回 HTTP 200。
  5. 设备动作完成后，调用 WES /api/v1/callback/result。
```

最小 ACK 响应：

```json
{
  "code": 200,
  "message": "OK"
}
```
