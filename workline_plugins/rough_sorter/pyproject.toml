[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "wes-rough-sorter-plugin"
version = "1.0.0"
description = "Rough sorter business decisions and WES application integration"
requires-python = ">=3.13"
dependencies = ["wes-plugin-sdk==0.1.0"]

[tool.uv.sources]
wes-plugin-sdk = { path = "../../src/wes_plugin_sdk" }

[dependency-groups]
dev = [
    "basedpyright>=1.28.0",
    "pytest>=9.0.3",
    "ruff>=0.15.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/rough_sorter"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src", "../.."]
addopts = ["-q"]

[tool.ruff]
target-version = "py313"
line-length = 120

[tool.ruff.lint]
select = [
    "B",
    "C4",
    "E",
    "F",
    "I",
    "N",
    "PERF",
    "PIE",
    "PL",
    "PT",
    "RET",
    "RSE",
    "RUF",
    "SIM",
    "UP",
]
ignore = [
    "PLR2004",
    "PLR0915",
    "PLC0415",
    "PT011",
    "PT018",
]

[tool.ruff.format]
quote-style = "double"

[tool.ruff.lint.per-file-ignores]
"src/rough_sorter/application/*.py" = ["PLR0912", "PLR0913", "RUF002"]

[tool.basedpyright]
pythonVersion = "3.13"
typeCheckingMode = "standard"
include = ["src"]
extraPaths = ["src", "../..", "../../src/wes_plugin_sdk/src"]
reportMissingImports = "error"
reportUndefinedVariable = "error"
