[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "wes-plugin-sdk"
version = "0.1.0"
description = "WES workline plugin stable facts, decisions, and read protocols"
requires-python = ">=3.13"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/wes_plugin_sdk"]
